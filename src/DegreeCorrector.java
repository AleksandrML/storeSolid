
// реализует принцип сегрегации интерфейсов: можно интегрировать в любой из созданных классов, где требуется (добавлен в алко классы)
public interface DegreeCorrector {
    void correctDegree(float degrees);
}
